server:
  port: 8011
  servlet:
    context-path: /user


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tonydon_blog
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-delete-field: delFlag
      logic-not-delete-value: 0
